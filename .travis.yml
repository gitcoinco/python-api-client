language: python

deploy:
  provider: pypi
  user: gitcoin
  password:
    secure: "placeholder"
  distributions: "sdist bdist_wheel"
  on:
    tags: true

matrix:
  include:
    - python: 3.4
    - python: 3.5
    - python: 3.6

install: pip install .

notifications:
  email: false
